# Next Human Review Gate

The next human dependency is **Mission 004 Human Gold #4**.

Before requesting reviewer input:
1. finalize Mission 004 packet content;
2. run packet contamination preflight;
3. compute and verify canonical SHA-256;
4. freeze packet ID as `mission-004-evidence-v1.0`;
5. update task, reviewer packet, Gold placeholder, and run template to that frozen ID;
6. keep all candidate responses embargoed.

Only then should the reviewer receive the Mission 004 reviewer-blind packet. After Gold #4 is frozen, external candidates may be run and compared against it.

Mission 005 and Mission 006 remain staged behind Mission 004 so the reviewer-blind Gold set grows in a controlled, auditable sequence rather than through parallel contamination risk.
